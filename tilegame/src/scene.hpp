#pragma once

#include "engine.hpp"

namespace tilegame
{
    // tilegame's aliases for the engine's generic scene classes, kept under the tilegame
    // namespace for consistency with everything else here (components::, systems::, ...).
    typedef engine::Scene Scene;
    typedef engine::SceneManager SceneManager;
} // namespace tilegame
